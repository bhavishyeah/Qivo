{
	"name": "qivo",
	"private": true,
	"scripts": {
		"dev:api": "cd apps/api && pnpm dev",
		"dev:web": "cd apps/web && pnpm dev",
		"build:api": "cd apps/api && pnpm exec tsc --outDir dist",
		"build:web": "cd apps/web && pnpm exec vite build",
		"db:migrate": "pnpm exec prisma migrate deploy",
		"db:generate": "pnpm exec prisma generate",
		"db:studio": "pnpm exec prisma studio",
		"lint": "cd apps/web && pnpm lint",
		"typecheck": "cd apps/api && pnpm exec tsc --noEmit && cd ../../apps/web && pnpm exec tsc --noEmit"
	},
	"dependencies": {
		"dotenv": "^17.4.2",
		"prisma": "^7.9.1"
	}
}
